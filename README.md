SAC implemented using NNs in PyTorch
-----------------------------------

This implementation only works with actions with bounds [-1, 1].

The hyperparameters are not the same from the paper and may need to be tuned.
